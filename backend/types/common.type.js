/**
 * @typedef {Object} UserRole
 * @property {number} id
 * @property {'user' | 'admin'} name
 */

/**
 * @typedef {Object} OrderStatus
 * @property {number} id
 * @property {'pending' | 'preparing' | 'ready'} name
 */

/**
 * @typedef {Object} DeliveryType
 * @property {number} id
 * @property {'pickup' | 'restaurant' | 'delivery'} name
 */

/**
 * @typedef {Object} OrderItemType
 * @property {number} id
 * @property {'dish' | 'custom'} name
 */